[build-system]
requires=["setuptools>=68"]
build-backend="setuptools.build_meta"

[project]
name="server-health-cli"
version="1.0.0"
description="CLI tool for monitoring system health."
readme="README.md"
requires-python=">=3.10"
dependencies=["psutil>=7.0.0"]
authors=[{name="Arian Salarian"}]
license={text="MIT"}
